# Social Network Prototype
Full-fledged prototype (template) of social network developed in Python using Flask. Also has a database.

There is registration and login, all data are stored locally in ```social.db```. It is possible to publish text posts and their reactions.

Social network runs on internal IP as well as ```127.0.0.1```.

*Warning! Use Flask only in development, never use for commercial purposes.*
